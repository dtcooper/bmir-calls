<?xml version="1.0" encoding="UTF-8"?>
<Response>
    {% if confirmed %}
        <Say>You are now enrolled to receive calls! Good-bye.</Say>
        <Play>{{ song_url }}</Play>
        <Hangup />
    {% else %}
        {% set max_gather_times = 4 %}
        {% if gather_times > max_gather_times %}
            <Hangup />
        {% else %}
            <Gather timeout="5" numDigits="1" finishOnKey="" actionOnEmptyResult="true" action="{{ action_url }}">
                <Say>
                {% if gather_times == 1 %}
                    Hi {{ name }}. Welcome to the B M I R F M phone experiment.
                {% else %}
                </Say>
                    <Play>{{ song_url }}</Play>
                <Say>
                {% endif %}
                If you would like to participate
                {%- if gather_times > 1 %}
                    in the B M I R phone experiment
                {%- endif %}, press 1.
                If you were not expecting this call, hang up.
                {% if gather_times < max_gather_times %}
                    To repeat this message, press any other key.
                {% endif %}
                </Say>
            </Gather>
        {% endif %}
    {% endif %}
</Response>
