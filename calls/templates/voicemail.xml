<?xml version="1.0" encoding="UTF-8"?>
<Response>
     <Say>
        Welcome to R M I B, 54 dot 9 AM.
        No one is available to call your take right now.
        Beep until the message to leave your please wait.
        Metamorphosized? Have I metamorphosed?
        I can't remember. Things have changed since we last spoke.
        We've never spoke. Message. Beep.
    </Say>
    <Record
        maxLength="150"
        timeout="3"
        trim="trim-silence"
        action="{{ action_url }}"
        transcribeCallback="{{ transcribe_callback_url }}"
    />
</Response>
