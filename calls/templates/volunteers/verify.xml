<Response>
  {% if reject %}
    <Hangup />
  {% else %}
    {% if confirmed %}
      <Say voice="alice">You are now enrolled to receive calls!</Say>
      <Play>{{ song_url }}</Play>
      <Hangup />
    {% else %}
      {% if first_run %}
        <Say voice="alice">
          Welcome to the B. M. I. R. F. M. phone experiment, {{ name }}.
        </Say>
      {% endif %}
      <Gather timeout="5" numDigits="1" finishOnKey="" actionOnEmptyResult="true" action="{{ action_url }}">
        <Say voice="alice">
          If you would like to participate
          {%- if not first_run %}
            in the B. M. I. R. phone experiment
          {%- endif %}, press 1.
          If you were not expecting this call, hang up.
          To repeat this message, press any other key.
        </Say>
      </Gather>
    {% endif %}
  {% endif %}
</Response>
